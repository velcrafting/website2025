---
title: MDX Feature Showcase
summary: A compact tour of frontmatter, KPIs, emoji, code, tables, and JSX callouts.
hero: "/writing/ai-moderation-trends/hero.jpg"
ogImage: "/writing/ai-moderation-trends/og.jpg"
date: 2025-05-31
kpi:
  - value: "70%"
    label: "Better Readability"
  - value: "100+"
    label: "Articles"
  - value: "Global"
    label: "SEO Values"
tags: [demo, docs]
---

import Callout from "@/components/mdx/Callout";

## Headings, emphasis, emoji

Use **bold**, _italic_, `inline code`, and ~~strikethrough~~.  
Emoji shortcodes work too :rocket: :tada:.

<Callout type="note" title="What this page proves">
This page renders through your MDX toolchain with frontmatter stripped and exported.
</Callout>

<Callout type="tip" title="Authoring tip">
Prefer Markdown for content. Use lightweight JSX as needed.
</Callout>

## Task list and table (GFM)

- [x] Frontmatter exported and not duplicated in body
- [x] KPIs render from `frontmatter.kpi`
- [x] GFM tables and task lists
- [ ] Ship a second case study

| Metric         | Before | After | Δ    |
| -------------- | -----: | ----: | ---: |
| Response time  | 120 ms |  36 ms| −70% |
| Alerts handled |  0.6M  |  1.1M | +83% |

## Code blocks with titles

```ts title="utils/score.ts"
// TypeScript example
export function normalizeScore(x: number, min = 0, max = 100) {
  if (max === min) return 0;
  const clamped = Math.min(Math.max(x, min), max);
  return (clamped - min) / (max - min);
}
